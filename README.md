# sitectl-wp

A [sitectl](https://sitectl.libops.io) plugin for WordPress utilities. It works with the [LibOps WordPress template](https://github.com/libops/wp).

The docs are available at [https://sitectl.libops.io/plugins/wordpress](https://sitectl.libops.io/plugins/wordpress).
